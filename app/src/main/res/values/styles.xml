<resources>
    <!-- Keep any existing styles -->
    
    <style name="WhiteSpinnerStyle" parent="Widget.AppCompat.Spinner">
        <item name="android:background">@drawable/input</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textSize">16sp</item>
        <item name="android:popupBackground">@color/colorGray</item>
        <item name="android:dropDownSelector">@color/colorWhite</item>
        <item name="android:minHeight">48dp</item>
    </style>
    
    <!-- Keep your existing SpinnerTheme -->
</resources>